<!--
本文件由智谱 AI 自动翻译生成
原文件: SKILL.md
翻译时间: 2025-12-12 16:13:09
翻译模型: glm-4-flash
原文大小: 25,533 字符
-->

---
name: pptx
description: "演示文稿创建、编辑和分析。当Claude需要处理演示文稿（.pptx文件）时，用于：(1) 创建新的演示文稿，(2) 修改或编辑内容，(3) 处理布局，(4) 添加注释或演讲者备注，或任何其他演示文稿任务"
license: Proprietary. LICENSE.txt has complete terms
---

# PPTX创建、编辑和分析

## 概述

用户可能要求您创建、编辑或分析演示文稿的内容。.pptx文件本质上是一个包含XML文件和其他资源的ZIP存档，您可以读取或编辑。您有不同工具和工作流程可用于不同的任务。

## 读取和分析内容

### 文本提取
如果您只需要读取演示文稿的文本内容，应将文档转换为Markdown：

```bash
# 将文档转换为Markdown
python -m markitdown 路径-to-file.pptx
```

### 原始XML访问
您需要原始XML访问权限以获取：注释、演讲者备注、幻灯片布局、动画、设计元素和复杂格式。对于这些功能中的任何一项，您都需要解包演示文稿并读取其原始XML内容。

#### 解包文件
`python ooxml/scripts/unpack.py <office_file> <output_dir>`

**注意**：unpack.py脚本位于项目根目录相对于`skills/pptx/ooxml/scripts/unpack.py`的位置。如果脚本不存在于此路径，请使用`find . -name "unpack.py"`来定位它。

#### 关键文件结构
* `ppt/presentation.xml` - 主要演示文稿元数据和幻灯片引用
* `ppt/slides/slide{N}.xml` - 单个幻灯片内容（slide1.xml、slide2.xml等）
* `ppt/notesSlides/notesSlide{N}.xml` - 每个幻灯片的演讲者备注
* `ppt/comments/modernComment_*.xml` - 特定幻灯片的注释
* `ppt/slideLayouts/` - 幻灯片布局模板
* `ppt/slideMasters/` - 主幻灯片模板
* `ppt/theme/` - 主题和样式信息
* `ppt/media/` - 图片和其他媒体文件

#### 字体和颜色提取
**当给定一个要仿制的示例设计时**：始终使用以下方法首先分析演示文稿的字体和颜色：
1. **读取主题文件**：检查`ppt/theme/theme1.xml`中的颜色（`<a:clrScheme>`）和字体（`<a:fontScheme>`）
2. **检查示例幻灯片内容**：检查`ppt/slides/slide1.xml`中的实际字体使用（`<a:rPr>`）和颜色
3. **搜索模式**：使用grep在所有XML文件中查找颜色（`<a:solidFill>`、`<a:srgbClr>`）和字体引用

## 创建新的PowerPoint演示文稿（不使用模板）

当从头开始创建新的PowerPoint演示文稿时，使用**html2pptx**工作流程将HTML幻灯片转换为PowerPoint，并保持准确的定位。

### 设计原则

**至关重要**：在创建任何演示文稿之前，分析内容并选择适当的设计元素：
1. **考虑主题内容**：这个演示文稿是关于什么的？它暗示了什么语气、行业或氛围？
2. **检查品牌**：如果用户提到了公司/组织，请考虑他们的品牌颜色和标识
3. **匹配调色板到内容**：选择反映主题的颜色
4. **说明你的方法**：在编写代码之前解释你的设计选择

**要求**：
- ✅ 在编写代码之前，说明你的内容导向的设计方法
- ✅ 只使用Web安全字体：Arial、Helvetica、Times New Roman、Georgia、Courier New、Verdana、Tahoma、Trebuchet MS、Impact
- ✅ 通过大小、粗细和颜色创建清晰的视觉层次结构
- ✅ 确保可读性：强烈的对比、适当大小的文本、干净的排列
- ✅ 保持一致性：重复图案、间距和视觉语言跨幻灯片

#### 色彩调色板选择

**创造性地选择颜色**：
- **超越默认值**：什么颜色真正匹配这个特定主题？避免自动选择。
- **考虑多个角度**：主题、行业、氛围、能量水平、目标受众、品牌标识（如果提到）
- **大胆尝试**：尝试意外的组合 - 医疗保健演示文稿不必是绿色，金融不必是深蓝色
- **构建你的调色板**：选择3-5种一起工作的颜色（主色 + 支持色调 + 强调色）
- **确保对比度**：文本必须清楚地可读于背景上

**示例色彩调色板**（用于激发创造力 - 选择一个，根据它调整，或创建自己的调色板）：

1. **经典蓝色**：深蓝色（#1C2833）、蓝灰色（#2E4053）、银色（#AAB7B8）、米白色（#F4F6F6）
2. **蓝绿色与珊瑚色**：蓝绿色（#5EA8A7）、深蓝绿色（#277884）、珊瑚色（#FE4447）、白色（#FFFFFF）
3. **鲜艳的红色**：红色（#C0392B）、鲜红色（#E74C3C）、橙色（#F39C12）、黄色（#F1C40F）、绿色（#2ECC71）
4. **温暖的桃红色**：淡紫色（#A49393）、桃红色（#EED6D3）、玫瑰色（#E8B4B8）、奶油色（#FAF7F2）
5. **酒红色奢华**：酒红色（#5D1D2E）、猩红色（#951233）、锈色（#C15937）、金色（#997929）
6. **深紫色与翡翠绿**：紫色（#B165FB）、深蓝色（#181B24）、翡翠绿（#40695B）、白色（#FFFFFF）
7. **奶油色与森林绿**：奶油色（#FFE1C7）、森林绿（#40695B）、白色（#FCFCFC）
8. **粉色与紫色**：粉色（#F8275B）、珊瑚色（#FF574A）、玫瑰色（#FF737D）、紫色（#3D2F68）
9. **酸橙与梅子色**：酸橙（#C5DE82）、梅子色（#7C3A5F）、珊瑚色（#FD8C6E）、蓝灰色（#98ACB5）
10. **黑色与金色**：金色（#BF9A4A）、黑色（#000000）、奶油色（#F4F6F6）
11. **鼠尾草与赭色**：鼠尾草（#87A96B）、赭色（#E07A5F）、奶油色（#F4F1DE）、炭灰色（#2C2C2C）
12. **炭灰色与红色**：炭灰色（#292929）、红色（#E33737）、浅灰色（#CCCBCB）
13. **鲜艳的橙色**：橙色（#F96D00）、浅灰色（#F2F2F2）、炭灰色（#222831）
14. **森林绿**：黑色（#191A19）、绿色（#4E9F3D）、深绿色（#1E5128）、白色（#FFFFFF）
15. **复古彩虹**：紫色（#722880）、粉色（#D72D51）、橙色（#EB5C18）、琥珀色（#F08800）、金色（#DEB600）
16. **复古土色**：芥末色（#E3B448）、鼠尾草（#CBD18F）、森林绿（#3A6B35）、奶油色（#F4F1DE）
17. **海岸玫瑰**：旧玫瑰（#AD7670）、海狸色（#B49886）、蛋壳色（#F3ECDC）、灰烬色（#BFD5BE）
18. **橙色与青绿色**：浅橙色（#FC993E）、灰绿色（#667C6F）、白色（#FCFCFC）

#### 视觉细节选项

**几何图案**：
- 对角线分隔符而不是水平分隔符
- 不对称的列宽（30/70、40