# Skill Box 技能添加指南

本指南规范向 Skill Box 仓库添加新技能的完整流程。请从上至下按步骤执行。

---

## 📥 Phase 0: 外部技能仓库导入 (可选)

如果技能来自外部GitHub仓库，先克隆到临时目录：

```powershell
# 1. 创建临时克隆目录
mkdir skills_incoming
cd skills_incoming

# 2. 克隆外部仓库
git clone https://github.com/author/repo-name.git
git clone https://github.com/another-author/another-repo.git

# 3. 返回项目根目录
cd ..
```

**探索技能结构**:
```powershell
# 查看克隆的仓库结构
Get-ChildItem -Recurse skills_incoming | Where-Object {$_.Name -eq "SKILL.md"}
```

---

## 📋 Phase 1: 技能分析与策略设计

### 1.1 分析技能结构

阅读每个技能的 `SKILL.md`，了解：
- **功能描述** - 技能的核心能力
- **使用场景** - 何时触发此技能
- **依赖关系** - 是否依赖其他技能（如AI后端）
- **原作者信息** - 来源仓库和作者

### 1.2 设计添加方案

根据技能特点选择合适的策略：

| 技能特点 | 推荐策略 | 示例 |
|----------|----------|------|
| **独立单一功能** | 创建独立插件 | `invoice-processor` |
| **功能高度相关** | 合并为套件插件 | `document-suite` (docx/pdf/pptx/xlsx) |
| **有依赖关系** | 合并为工作流插件 | `baoyu-content-creation-suite` (content + ai-generation) |
| **工具类技能** | 分别创建独立插件 | `baoyu-x-to-markdown`, `baoyu-url-to-markdown` |

**方案C (混合策略)** - 推荐用于复杂技能组：
- 整合核心工作流为1个大套件
- 拆分独立工具为多个小插件

### 1.3 确定技能分类

根据功能将技能归入以下分类：

| 文件夹名 | 类别名称 | 说明 |
|---------|----------|------|
| `no-code-builder` | 零代码构建 | 编程、代码生成、测试、部署 |
| `office-automation` | 办公自动化 | Word, Excel, PPT, PDF, 文件管理 |
| `content-pipeline` | 内容流水线 | 爬虫、下载、转录、内容创作 |
| `immersive-reading` | 沉浸式研读 | 阅读分析、笔记、学习计划 |
| `visual-creative` | 视觉与创意 | 绘图、设计、图像处理、视频制作 |
| `brand-marketing` | 品牌与营销 | 品牌规范、文案、营销分析 |
| `business-analyst` | 商业分析师 | 数据分析、财务建模、报表 |

---

## 📦 Phase 2: 文件迁移

### 2.1 移动技能文件夹

```powershell
# 示例：移动baoyu技能到content-pipeline
Move-Item -Path ".\skills_incoming\baoyu-skills\skills\baoyu-*" -Destination ".\content-pipeline\" -Force

# 示例：移动remotion到visual-creative
Move-Item -Path ".\skills_incoming\skills\skills\remotion" -Destination ".\visual-creative\remotion"
```

### 2.2 验证文件结构

确保每个技能文件夹：
- ✅ 直接包含 `SKILL.md` 文件（必须）
- ✅ **保持根目录整洁**：除 `SKILL.md` (及可选的 `skill.json`/`skill.yaml`) 外，所有 Markdown 文档（README, CHANGELOG, 指南等）**必须**移动到 `docs/` 子目录中
- ✅ `SKILL.md` 中的链接已更新指向 `docs/` (如 `[Link](docs/guide.md)`)
- ✅ 保留完整的子目录（如 `rules/`, `examples/`, `scripts/`）
- ✅ 路径正确（如 `./content-pipeline/baoyu-xhs-images/`）

```powershell
# 验证技能已正确移动
Test-Path ".\content-pipeline\baoyu-xhs-images\SKILL.md"
```

---

## 🌍 Phase 3: 翻译与多语言同步

为了方便中文用户理解及长期知识管理，Skill Box 使用一套自动化的 i18n 系统。

### 3.1 核心文档翻译 (SKILL.zh.md)

在 `i18n/skills/` 目录下根据技能所属路径创建对应的翻译文件。

```powershell
# 示例：为 supabase-postgres-best-practices 创建翻译文件
# 路径应与原技能路径一一对应
New-Item -Path ".\i18n\skills\no-code-builder\supabase-postgres-best-practices\SKILL.zh.md" -Force
```

**翻译准则**:
1.  **翻译 SKILL.md**: 将原始 `SKILL.md` 完整翻译为中文。
2.  **保留 Frontmatter**: 保持文件顶部的 YAML 元数据（--- 之间的内容）中的字段名不翻译。

### 3.2 自动化 i18n 注册 (i18n/locales/)

Skill Box 提供了脚本自动将新技能同步到多语言配置文件中：

```powershell
# 1. 同步 marketplace.json 到 i18n 分辨表
node i18n/scripts/translate-i18n.js
```

**手动校验**:
运行脚本后，需检查 `i18n/locales/marketplace.zh.json`，确保插件名和描述已正确翻译（如果之前没有 API Key，脚本可能生成 `[待翻译]` 占位符，需手动补全）。

---

## 🔧 Phase 4: 注册技能到 marketplace.json

### 3.1 添加插件条目

选择以下情况之一：

#### 情况 A: 添加到现有插件

如果技能属于现有套件，添加到 `skills` 数组：

```json
{
  "name": "data-analysis-toolkit",
  "skills": [
    "./business-analyst/csv-data-summarizer",
    "./business-analyst/excel-pivot-wizard",
    "./business-analyst/excel-variance-analyzer",
    "./business-analyst/new-analyzer"  // <--- 添加新技能
  ]
}
```

#### 情况 B: 创建新插件 (单作者)

```json
{
  "name": "plugin-name-kebab-case",
  "description": "Brief description of core capabilities in English.",
  "category": "category-folder-name",
  "author": {
    "name": "Author Name",
    "url": "https://github.com/author/repo"
  },
  "skills": [
    "./category/skill-folder-name"
  ]
}
```

#### 情况 C: 创建新插件 (多作者)

如果插件整合了多个来源的技能：

```json
{
  "name": "plugin-name",
  "description": "Brief description.",
  "category": "category-name",
  "authors": [
    {
      "name": "Author 1",
      "url": "https://github.com/author1/repo"
    },
    {
      "name": "Author 2",
      "url": "https://github.com/author2/repo"
    }
  ],
  "skills": [
    "./category/skill-from-author1",
    "./category/skill-from-author2"
  ]
}
```

### 3.2 Author字段规范

#### 单作者插件 - 使用 `author` (对象)

```json
"author": {
  "name": "Anthropic",
  "url": "https://github.com/anthropics/skills"
}
```

#### 多作者插件 - 使用 `authors` (数组)

**使用场景**：
- 插件整合了多个来源的技能
- 技能来源均分（如2:2, 1:1:1）
- 需要公平标注所有贡献者

```json
"authors": [
  {"name": "Anthropic", "url": "https://github.com/anthropics/skills"},
  {"name": "Vercel Labs", "url": "https://github.com/vercel-labs/agent-skills"}
]
```

#### 合并插件的Author归属原则

当多个技能合并为一个插件时：

| 情况 | 归属策略 | 示例 |
|------|----------|------|
| **100%同一作者** | 单一author | document-suite → Anthropic |
| **主要贡献者明确** | 单一author (主要) | data-analysis-toolkit → Jeremy Longshore (2/3) |
| **平分或难以判断** | 多个authors | frontend-builder → Anthropic & Vercel Labs |
| **3+来源** | 多个authors (按贡献排序) | testing-toolkit → ComposioHQ, obra, mhattingpete |

### 3.3 Description规范

**标准格式**:
- ✅ **英文描述** - 保持国际化
- ✅ **无emoji** - 保持专业简洁
- ✅ **无来源链接** - 来源信息在author.url中
- ✅ **功能导向** - 描述核心能力和使用场景

**示例**:

❌ **不推荐**:
```json
"description": "🎨 完整内容创作工作流。来源: https://github.com/..."
```

✅ **推荐**:
```json
"description": "Complete content creation workflow: AI image generation, multi-platform publishing, and article illustrations."
```

### 3.4 更新版本号

每当向仓库添加、移除或重组技能时，**必须**在 `marketplace.json` 中更新版本号。

```json
{
  "name": "Skill-Box",
  "version": "2.1.0",  // <--- 根据改动规模进行语义化版本更新 (MAJOR.MINOR.PATCH)
  ...
}
```

- **PATCH (x.x.1)**: 小改动或单个技能描述微调。
- **MINOR (x.1.0)**: 新增技能或插件（正如本次集成）。
- **MAJOR (1.0.0)**: 架构重构或大规模改版。

---

## 📝 Phase 5: 更新 README.md

### 4.1 更新顶部徽章

```markdown
<img src="https://img.shields.io/badge/Plugins-35-blue?style=for-the-badge" alt="Plugins">
<img src="https://img.shields.io/badge/Skills-64-orange?style=for-the-badge" alt="Skills">
```

**计算方法**:
- Plugins数 = marketplace.json 中的 `plugins` 数组长度
- Skills数 = 所有插件的 `skills` 数组元素总数

### 4.2 更新分类统计表

```markdown
| 类别 | 说明 | 插件数量 | 包含技能 |
|------|------|:--------:|:--------:|
| **内容流水线** | 网页采集、视频下载、AI内容创作全流程 | 6 | 18 |
```

### 4.3 更新完整列表

```markdown
<summary>点击展开 35 个插件 / 64 个技能详细清单</summary>

### 内容流水线 (Content Pipeline)
- **content-harvester**: `video-downloader`, `web-fetch`, `youtube-transcript`
- **content-research-writer**: 研究型写作助手
- **baoyu-content-creation-suite**: `xhs-images`, `post-to-x`, ... (来源: [JimLiu/baoyu-skills](URL))
```

**格式说明**:
- 新增插件加入对应分类
- 如来自外部，标注来源链接
- 使用反引号标注技能简称

---

## ✅ Phase 6: 验证与测试

### 5.1 JSON格式验证

```powershell
# 验证JSON语法
python -c "import json; json.load(open('.claude-plugin/marketplace.json', encoding='utf-8')); print('✓ JSON格式正确')"
```

### 5.2 Author字段完整性检查

```powershell
# 检查所有插件是否包含author或authors字段
python -c "import json; data = json.load(open('.claude-plugin/marketplace.json', encoding='utf-8')); missing = [p['name'] for p in data['plugins'] if 'author' not in p and 'authors' not in p]; print(f'缺少author字段: {len(missing)}个'); print('\n'.join(missing) if missing else '✓ 所有插件都有作者信息')"
```

### 5.3 统计验证

```powershell
# 统计插件和技能数量
python -c "import json; data = json.load(open('.claude-plugin/marketplace.json', encoding='utf-8')); plugins = len(data['plugins']); skills = sum(len(p['skills']) for p in data['plugins']); print(f'插件总数: {plugins}'); print(f'技能总数: {skills}')"
```

### 5.4 作者贡献统计

```powershell
# 查看各作者贡献的插件数量
python -c "from collections import Counter; import json; data = json.load(open('.claude-plugin/marketplace.json', encoding='utf-8')); authors = []; [authors.extend([a['name'] for a in p.get('authors', [])]) if 'authors' in p else authors.append(p.get('author', {}).get('name', 'Unknown')) for p in data['plugins']]; stats = Counter(authors); [print(f'{name}: {count}个插件') for name, count in stats.most_common()]"
```

---

## 📦 Phase 7: 清理与提交

### 6.1 清理临时文件

```powershell
# 删除克隆的临时仓库
Remove-Item -Recurse -Force .\skills_incoming
```

### 6.2 Git提交

```bash
# 添加所有更改
git add .

# 提交（使用约定式提交格式）
git commit -m "feat: add [Plugin/Skill Name] from [Source]

- Added X plugins with Y skills
- Updated README.md statistics
- Source: https://github.com/author/repo"

# 推送到远程
git push
```

---

## 📚 实际案例参考

### 案例 1: 导入 baoyu-skills (2026-01-25)

**技能数量**: 13个技能  
**策略**: 方案C (混合策略)  
**结果**: 
- 1个大套件 (`baoyu-content-creation-suite`, 10技能)
- 3个独立插件 (x-to-markdown, url-to-markdown, compress-image)

**关键决策**:
- content-skills + ai-generation-skills 合并 (有依赖关系)
- utility-skills 拆分为独立插件 (无依赖，单一功能)

### 案例 2: 导入 remotion-dev/skills (2026-01-25)

**技能数量**: 1个技能  
**策略**: 直接添加为独立插件  
**特点**: 包含46个规则文件的复杂技能

**文件结构保留**:
```
visual-creative/remotion/
├── SKILL.md
└── rules/  (46个最佳实践文档)
```

### 案例 3: 多作者插件处理

**插件**: `frontend-builder`  
**来源**: Anthropic (2技能) + Vercel Labs (2技能)  
**解决方案**: 使用 `authors` 数组公平标注

```json
"authors": [
  {"name": "Anthropic", "url": "https://github.com/anthropics/skills"},
  {"name": "Vercel Labs", "url": "https://github.com/vercel-labs/agent-skills"}
]
```

---

## 🔍 常见问题

### Q1: 如何判断是否应该合并技能为套件？

**合并场景**:
- ✅ 技能功能高度相关 (如docx/pdf/pptx/xlsx)
- ✅ 存在依赖关系 (如content需要ai-generation)
- ✅ 构成完整工作流 (如数据分析: csv→pivot→variance)

**保持独立场景**:
- ✅ 功能完全独立
- ✅ 使用场景不同
- ✅ 工具类单一功能

### Q2: Author字段应该用author还是authors？

**使用 `author` (对象)**:
- 单一来源的技能
- 合并插件的主要贡献者明确 (≥60%)

**使用 `authors` (数组)**:
- 多来源技能平分 (如2:2)
- 3个及以上来源
- 难以判断主次

### Q3: Description应该包含哪些信息？

**应该包含**:
- ✅ 核心功能描述
- ✅ 主要使用场景
- ✅ 技能数量（如：3个技能，46个最佳实践）

**不应该包含**:
- ❌ Emoji表情
- ❌ GitHub来源链接 (放在author.url)
- ❌ 中文内容 (保持国际化)

---

## 📊 检查清单

添加完成前，确认以下所有项：

- [ ] 技能文件已移动到正确的分类目录
- [ ] **根目录整洁验证**：确认根目录下只有 `SKILL.md`，其他md文件都在 `docs/` 中
- [ ] marketplace.json 添加了插件条目
- [ ] 已在 i18n/skills/ 目录下完成核心文档翻译 (SKILL.zh.md)
- [ ] 已在 i18n/locales/ 目录下完成多语言注册和翻译
- [ ] 插件包含 `author` 或 `authors` 字段
- [ ] Description为英文，无emoji，无来源链接
- [ ] README.md 顶部徽章数字已更新
- [ ] README.md 分类统计表已更新
- [ ] README.md 完整列表已添加新插件
- [ ] JSON格式验证通过
- [ ] Author字段完整性检查通过
- [ ] 临时文件已清理
- [ ] Git提交信息清晰完整

---

**更新记录**:
- 2026-02-26: 全面转向 i18n 自动化系统，弃用旧的 _translated 模式
- 2026-01-25: 基于baoyu-skills和remotion导入经验完善指南
- 初始版本: 基础流程框架
