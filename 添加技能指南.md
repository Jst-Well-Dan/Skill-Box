# Skill Box 技能添加指南

本以备忘录旨在规范向 Skill Box 仓库添加新技能的标准流程。请从上至下按步骤执行。

## 1. 技能分析与分类

首先阅读新技能文件夹中的 `SKILL.md`，了解其功能。
根据功能将其归入以下现有分类之一：

| 文件夹名 | 类别名称 | 说明 |
|---------|----------|------|
| `no-code-builder` | 零代码构建 | 编程、代码生成、测试、部署相关 |
| `office-automation` | 办公自动化 | Word, Excel, PPT, PDF, 文件管理 |
| `content-pipeline` | 内容流水线 | 爬虫、下载、转录、内容获取 |
| `immersive-reading` | 沉浸式研读 | 阅读分析、笔记、学习计划 |
| `visual-creative` | 视觉与创意 | 绘图、设计、图像处理、图表生成 |
| `brand-marketing` | 品牌与营销 | 品牌规范、文案、营销分析 |
| `business-analyst` | 商业分析师 | 数据分析、财务建模、报表 |

## 2. 文件迁移

将新技能的文件夹移动到对应的分类目录中。

```powershell
# 示例：移动到商业分析师目录
Move-Item -Path ".\SourceFolder\new-skill-name" -Destination ".\business-analyst\new-skill-name"
```

> **注意**：确保移动后的文件夹内直接包含 `SKILL.md` 文件。

## 3. 注册技能 (marketplace.json)

编辑 `.claude-plugin/marketplace.json` 文件，在 `plugins` 数组末尾添加新条目。

**数据模板：**

```json
{
  "name": "skill-folder-name",
  "description": "简短的一句话描述，通常来自 SKILL.md 的 description 字段。包含触发词说明。",
  "source": "./",
  "category": "category-folder-name",
  "author": {
    "name": "Author Name",
    "url": "https://github.com/author-profile-or-repo"
  },
  "skills": [
    "./category-folder-name/skill-folder-name"
  ]
}
```

*   `name`: 技能文件夹名称
*   `category`: 步骤 1 中确定的分类文件夹名
*   `skills`: 指向 SKILL.md 所在目录的相对路径

## 4. 更新文档 (README.md)

需要更新 `README.md` 中的四个位置：

1.  **徽章计数 (顶部)**
    *   找到 `<img src="...Skills-XX-orange..."`
    *   将 `XX` 更新为最新总数。

2.  **分类统计表 (## 技能分区)**
    *   更新对应类别的"技能数量"列。

3.  **完整清单折叠栏 (## 完整技能清单)**
    *   更新 `<summary>点击展开全部 XX 个技能</summary>` 中的数字。
    *   在对应的类别标题下，按字母顺序插入新技能：
        *   格式：`- **skill-name** - 中文简短描述`

4.  **致谢 (## 致谢, 可选)**
    *   如果是新的贡献者/作者，在表格中添加一行作者信息。

## 5. 验证与提交

1.  **清理**：删除来源文件夹（如 `skills_Incoming` 或克隆的临时仓库）。
2.  **验证**：确保所有 JSON 格式正确，路径无误。
3.  **Git 提交**：

```bash
git add .
git commit -m "feat: add [Skill Name] to [Category]"
git push
```
